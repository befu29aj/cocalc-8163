% gc-lab-07-derivatives.tex

\documentclass[11pt]{article}
\usepackage{enumerate}
\usepackage{syllogism} 
\usepackage{october}
\usepackage[table]{xcolor}

\newcounter{aufg}
\setcounter{aufg}{0}
\newcommand{\aufgabe}[0]{\refstepcounter{aufg}\textbf{(\arabic{aufg})}}

\begin{document}

\textbf{Derivatives}

\emph{\CourseName, \CourseNumber}

{\aufgabe} Find the tangent line for the following function at the given
point.
\begin{equation}
  \label{eq:ahraeboh}
  f(x)=\frac{x^{2}-1}{x^{2}+x+1}\mbox{ at }(1,0)
\end{equation}

{\aufgabe} Find the tangent line for the following curve at the given
point.
\begin{equation}
  \label{eq:shohxied}
y=-2\cos^{2}x+5\cos{}x\mbox{ at }x=\frac{\pi}{2}
\end{equation}

{\aufgabe} Find the tangent line for the following curve at the given
point.
\begin{equation}
  \label{eq:doiwaqui}
y=\sin(\sin{}x)\mbox{ at }(\pi,0)
\end{equation}

{\aufgabe} Find the tangent line for the following function at the given
point.
\begin{equation}
  \label{eq:saepowei}
f(x)=xe^{x}\mbox{ at }x=1
\end{equation}

{\aufgabe} Find the tangent line for the following function at the given
point.
\begin{equation}
  \label{eq:husiraim}
g(z)=\ln\left(z^{2}+1\right)\mbox{ at }z=0
\end{equation}

{\aufgabe} Find
\begin{equation}
  \label{eq:xoidahgh}
  \frac{d}{dx}\sqrt{2x^{2}-1}
\end{equation}

{\aufgabe} Find
\begin{equation}
  \label{eq:miazosho}
  \frac{d}{dx}\cos\left(3x^{2}\right)
\end{equation}

{\aufgabe} Find
\begin{equation}
  \label{eq:ahyohweo}
  \frac{d}{dx}\sqrt{\log_{2}3x}
\end{equation}



% \begin{figure}[h]
% \includegraphics[scale=.6]{./diagrams/prob02.jpg}
% \end{figure}

\end{document}

